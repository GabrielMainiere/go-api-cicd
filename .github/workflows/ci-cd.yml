name: go-api-ci-cd

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  ci:
    name: CI - Build, Test, Lint, Quality, SAST
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: appdb
          POSTGRES_USER: appuser
          POSTGRES_PASSWORD: apppass
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U appuser -d appdb"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=10
    env:
      DATABASE_URL: postgres://appuser:apppass@localhost:5432/appdb?sslmode=disable
      RUN_INTEGRATION_TESTS: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Download dependencies
        run: go mod tidy

      - name: Build
        run: go build ./...

      - name: Unit tests
        run: go test ./...

      - name: Integration tests
        run: go test -tags=integration ./...

      - name: Coverage
        run: go test ./... -coverprofile=coverage.out

      - name: Lint (golangci-lint)
        uses: golangci/golangci-lint-action@v6
        with:
          version: latest

      - name: SonarQube Scan
        if: ${{ secrets.SONAR_TOKEN != '' && secrets.SONAR_HOST_URL != '' }}
        uses: SonarSource/sonarqube-scan-action@v5
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}

      - name: SAST (Semgrep)
        uses: semgrep/semgrep-action@v1
        with:
          config: p/ci

  container:
    name: Container - Lint, Build, Scan, Push
    if: github.ref == 'refs/heads/main'
    needs: ci
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Hadolint
        uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: Dockerfile

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build image for scan
        uses: docker/build-push-action@v6
        with:
          context: .
          push: false
          load: true
          tags: go-api-cicd:${{ github.sha }}

      - name: Trivy image scan
        uses: aquasecurity/trivy-action@0.24.0
        with:
          image-ref: go-api-cicd:${{ github.sha }}
          format: table
          exit-code: 1
          ignore-unfixed: true
          vuln-type: os,library
          severity: CRITICAL,HIGH

      - name: Docker Hub Login
        if: ${{ secrets.DOCKERHUB_USERNAME != '' && secrets.DOCKERHUB_TOKEN != '' }}
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push image
        if: ${{ secrets.DOCKERHUB_USERNAME != '' && secrets.DOCKERHUB_TOKEN != '' }}
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/go-api-cicd:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/go-api-cicd:${{ github.sha }}

  deploy_hml:
    name: CD - Deploy Homolog + DAST
    if: github.ref == 'refs/heads/main'
    needs: container
    runs-on: ubuntu-latest
    steps:
      - name: Deploy Render Homolog
        if: ${{ secrets.RENDER_HML_DEPLOY_HOOK != '' }}
        run: curl -X POST "${{ secrets.RENDER_HML_DEPLOY_HOOK }}"

      - name: DAST (OWASP ZAP Baseline)
        if: ${{ secrets.HML_APP_URL != '' }}
        uses: zaproxy/action-baseline@v0.14.0
        with:
          target: ${{ secrets.HML_APP_URL }}

  deploy_prod:
    name: CD - Deploy Production
    if: github.ref == 'refs/heads/main'
    needs: deploy_hml
    runs-on: ubuntu-latest
    environment:
      name: production
      url: ${{ secrets.PROD_APP_URL }}
    steps:
      - name: Deploy Render Production
        if: ${{ secrets.RENDER_PROD_DEPLOY_HOOK != '' }}
        run: curl -X POST "${{ secrets.RENDER_PROD_DEPLOY_HOOK }}"
